# HTTP server port
PORT=3000

# Runtime: development | production | test
NODE_ENV=development

# Sentry error monitoring (optional — leave empty to disable)
SENTRY_DSN=

# PostgreSQL — required for Prisma CLI and database connection (wired in M0-T02)
DATABASE_URL=postgresql://user:password@localhost:5432/relay
